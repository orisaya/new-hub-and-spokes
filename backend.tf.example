# ============================================================================
# BACKEND CONFIGURATION - EXAMPLE
# ============================================================================
# This project uses partial backend configuration with .tfbackend files
#
# Backend configuration is split into two parts:
# 1. Main backend block in versions.tf (already configured)
# 2. Environment-specific .tfbackend files (see below)
#
# ============================================================================

# Example .tfbackend file format (environments/dev/backend.tfbackend):
#
# resource_group_name  = "rg-terraform-state"
# storage_account_name = "tfstateXXXXXXXX"  # Replace with your storage account
# container_name       = "tfstate"
# key                  = "hub-spoke-dev.tfstate"

# To use:
# 1. Update storage_account_name in environments/dev/backend.tfbackend
# 2. Update storage_account_name in environments/prod/backend.tfbackend
# 3. Run: terraform init -backend-config=environments/dev/backend.tfbackend
#    Or use: make init-dev

# ============================================================================
# ALTERNATIVE: Local Backend (for testing only)
# ============================================================================
# If you want to use local state instead of Azure Storage, update versions.tf:
#
# terraform {
#   backend "local" {
#     path = "terraform.tfstate"
#   }
# }
#
# WARNING: Local backend is NOT recommended for production use!
# - No state locking (risk of corruption)
# - No team collaboration
# - No backup/versioning
# - State file stored locally (can be lost)
